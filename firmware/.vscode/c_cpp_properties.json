{
    "configurations": [
        {
            "name": "ESP-IDF",
            "includePath": [
                "${env:IDF_PATH}/components/**",
                "${workspaceFolder}/**"
            ],
            "browse": {
                "path": [
                    "${env:IDF_PATH}/components",
                    "${workspaceFolder}"
                ]
            }
        }
    ],
    "version": 4
}
